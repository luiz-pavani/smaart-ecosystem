{
  "framework": "nextjs",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "ignoreCommand": "bash -c 'if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ]; then git diff --quiet HEAD^ HEAD -- apps/titan/ || exit 1; else exit 1; fi'"
}
